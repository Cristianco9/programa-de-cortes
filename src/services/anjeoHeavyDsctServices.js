export const calculateHeavyWidth = (inputWidth) => {
  let outputWidth = inputWidth - 7.2;
  return outputWidth;
};

export const calculateHeavyHeight = (inputHeight) => {
  let outputHeight = inputHeight - 11.8;
  return outputHeight;
};

export const calculateHeavyWidthOneToAnother = (inputWidth) => {
  let outputWidth = (inputWidth - 10.9) / 2;
  return outputWidth;
};

export const calculateHeavyWidthDivisorOneToAnother = (inputWidth) => {
  let outputWidth = (inputWidth + 3.5) / 2;
  return outputWidth;
};

export const calculateHeavyWidthOneToAnotherTwo = (inputWidth) => {
  let outputWidth = (inputWidth + 7) / 3;
  return outputWidth;
};

export const calculateHeavyWidthPickUpCenter = (inputWidth) => {
  let outputWidth = (inputWidth - 14.4) / 2;
  return outputWidth;
};

export const calculateHeavyWidthDivisorPickUpCenter = (inputWidth) => {
  let outputWidth = (inputWidth / 2);
  return outputWidth;
};

export const calculateHeavyD28PickUpCenter = (inputHeight) => {
  let outputHeight = (inputHeight - 4.8);
  return outputHeight;
};

export const calculateHeavyWidthFixed = (inputWidth) => {
  let outputWidth = inputWidth - 7.2;
  return outputWidth;
};

export const calculatHeavyHeightFixed = (inputHeight) => {
  let outputHeight = inputHeight - 7.2;
  return outputHeight;
};

export const calculateHeavyWidthThreeShips = (inputWidth) => {
  let outputWidth = (inputWidth - 14.6) / 3;
  return outputWidth;
};

export const calculateHeavyWidthDivisorThreeShips = (inputWidth) => {
  let outputWidth = (inputWidth + 7) / 3;
  return outputWidth;
};

export const calculateHeavyWidthDivisorFourShips = (inputWidth) => {
  let outputWidth = (inputWidth + 7) / 4;
  return outputWidth;
};

export const calculateHeavyWidthFourShips = (inputWidth) => {
  let outputWidth = (inputWidth - 21.8) / 4;
  return outputWidth;
};
